const ipfs = require('ipfs-only-hash');

module.exports = {
  hashIpfs: async (input) => {
    try {
      return await ipfs.of(input);
    } catch (e) {
      throw new Error(`error hash file : ${e}`);
    }
  },
};
