const createTorrent = require('create-torrent');

module.exports = {
  createTorrentFile: (input, opts) => {
    return new Promise((resolve) => {
      createTorrent(input, opts, async (err, torrent) => {
        if (!err) {
          resolve(torrent);
        }
      });
    });
  },
};
